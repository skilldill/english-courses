export type Course = {
    id: string;
    title: string;
    description: string;
    topics: string[];
    poster: string;
    speaker: string;
    date: string;
    tags: string[];
};

export type CourseList = {
    id: string;
    title: string;
    poster: string;
    speaker: string;
    date: string;
}
